---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plexconfig-pvc1
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: syno-nfs
  volumeName: plexconfig-pv1
  resources:
    requests:
      storage: 500Mi
